port: 7880

log_level: debug

rtc:
  tcp_port: 7881
  port_range_start: 50100
  port_range_end: 50199
  # For Podman: Disable external IP to prevent using public IP
  use_external_ip: false
  # Use local network IP for WebRTC media connectivity
  # Replace with your machine's IP: run `ifconfig | grep "inet " | grep -v 127.0.0.1`
  node_ip: 10.9.225.111
  # TURN server configuration for NAT traversal
  turn_servers:
    - host: 10.9.225.111
      port: 3478
      protocol: udp
      username: "verse"
      credential: verse-turn-secret

redis:
  address: redis:6379

keys:
  verse-api-key: verse-api-secret-value-here-change-me

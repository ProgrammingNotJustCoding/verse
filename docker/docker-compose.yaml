include:
  # - ./compose/coturn/compose.yaml  # Not needed for local development
  - ./compose/livekit/compose.yaml
  - ./compose/minio/compose.yaml
  - ./compose/postgres/compose.yaml

networks:
  verse-network:
    driver: bridge
    name: verse-network
